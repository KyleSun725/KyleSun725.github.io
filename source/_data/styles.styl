// 全局字体设置
body {
  font-family: Consolas, Monaco, 'Andale Mono', 'Ubuntu Mono', monospace;
}

// 标题字体设置 (可选，如果想让标题也变)
h1, h2, h3, h4, h5, h6 {
  font-family: Consolas, Monaco, 'Andale Mono', 'Ubuntu Mono', monospace;
}

// 代码块字体设置 (Next 默认就是 monospace，这里加强一下)
code, pre {
  font-family: Consolas, Monaco, 'Andale Mono', 'Ubuntu Mono', monospace;
}